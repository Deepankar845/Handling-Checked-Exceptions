# Handling-Checked-Exceptions
A Java program that reads data from a file and implementing exception handling to catch 'FileNotFoundException' and 'IOException' also displaying appropriate error messages if the file does not exist or if there is an error reading the file.
